import MainButton from "../../../UI/atoms/button/main_button";

const StartWorkOrder = () => {
    return (
        <MainButton icon="fa-play">
            Başla
        </MainButton>
    );
};

export default StartWorkOrder;