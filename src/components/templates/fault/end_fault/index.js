import MainButton from "../../../UI/atoms/button/main_button";

const EndFault = () => {
    return (
        <MainButton icon="fa-stop">
            BİTİR
        </MainButton>
    );
};

export default EndFault;